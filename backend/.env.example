# Server Configuration
PORT=8080
GIN_MODE=release

# JWT Configuration
JWT_SECRET=change-this-to-a-secure-random-string-in-production
JWT_EXPIRY_HOURS=24

# Database (will be in /app/data for persistence)
DATABASE_PATH=/app/data/health_tracker.db
